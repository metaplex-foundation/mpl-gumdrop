//! This code was AUTOGENERATED using the kinobi library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun kinobi to update it.
//!
//! [https://github.com/metaplex-foundation/kinobi]
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_program::pubkey::Pubkey;

/// Accounts.
pub struct ProveClaim {
    /// The [MerkleDistributor].
    pub distributor: solana_program::pubkey::Pubkey,
    /// Status of the claim.
    pub claim_proof: solana_program::pubkey::Pubkey,
    /// Extra signer expected for claims
    pub temporal: solana_program::pubkey::Pubkey,
    /// Payer of the claim.
    pub payer: solana_program::pubkey::Pubkey,
    /// The [System] program.
    pub system_program: solana_program::pubkey::Pubkey,
}

impl ProveClaim {
    pub fn instruction(
        &self,
        args: ProveClaimInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ProveClaimInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.distributor,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.claim_proof,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.temporal,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.payer, true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ProveClaimInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::GUMDROP_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshDeserialize, BorshSerialize)]
struct ProveClaimInstructionData {
    discriminator: [u8; 8],
}

impl ProveClaimInstructionData {
    fn new() -> Self {
        Self {
            discriminator: [52, 82, 123, 224, 40, 139, 230, 184],
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ProveClaimInstructionArgs {
    pub claim_prefix: Vec<u8>,
    pub claim_bump: u8,
    pub index: u64,
    pub amount: u64,
    pub claimant_secret: Pubkey,
    pub resource: Pubkey,
    pub resource_nonce: Vec<u8>,
    pub proof: Vec<[u8; 32]>,
}

/// Instruction builder for `ProveClaim`.
///
/// ### Accounts:
///
///   0. `[writable]` distributor
///   1. `[writable]` claim_proof
///   2. `[signer]` temporal
///   3. `[writable, signer]` payer
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Default)]
pub struct ProveClaimBuilder {
    distributor: Option<solana_program::pubkey::Pubkey>,
    claim_proof: Option<solana_program::pubkey::Pubkey>,
    temporal: Option<solana_program::pubkey::Pubkey>,
    payer: Option<solana_program::pubkey::Pubkey>,
    system_program: Option<solana_program::pubkey::Pubkey>,
    claim_prefix: Option<Vec<u8>>,
    claim_bump: Option<u8>,
    index: Option<u64>,
    amount: Option<u64>,
    claimant_secret: Option<Pubkey>,
    resource: Option<Pubkey>,
    resource_nonce: Option<Vec<u8>>,
    proof: Option<Vec<[u8; 32]>>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl ProveClaimBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [MerkleDistributor].
    #[inline(always)]
    pub fn distributor(&mut self, distributor: solana_program::pubkey::Pubkey) -> &mut Self {
        self.distributor = Some(distributor);
        self
    }
    /// Status of the claim.
    #[inline(always)]
    pub fn claim_proof(&mut self, claim_proof: solana_program::pubkey::Pubkey) -> &mut Self {
        self.claim_proof = Some(claim_proof);
        self
    }
    /// Extra signer expected for claims
    #[inline(always)]
    pub fn temporal(&mut self, temporal: solana_program::pubkey::Pubkey) -> &mut Self {
        self.temporal = Some(temporal);
        self
    }
    /// Payer of the claim.
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_program::pubkey::Pubkey) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// The [System] program.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_program::pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn claim_prefix(&mut self, claim_prefix: Vec<u8>) -> &mut Self {
        self.claim_prefix = Some(claim_prefix);
        self
    }
    #[inline(always)]
    pub fn claim_bump(&mut self, claim_bump: u8) -> &mut Self {
        self.claim_bump = Some(claim_bump);
        self
    }
    #[inline(always)]
    pub fn index(&mut self, index: u64) -> &mut Self {
        self.index = Some(index);
        self
    }
    #[inline(always)]
    pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.amount = Some(amount);
        self
    }
    #[inline(always)]
    pub fn claimant_secret(&mut self, claimant_secret: Pubkey) -> &mut Self {
        self.claimant_secret = Some(claimant_secret);
        self
    }
    #[inline(always)]
    pub fn resource(&mut self, resource: Pubkey) -> &mut Self {
        self.resource = Some(resource);
        self
    }
    #[inline(always)]
    pub fn resource_nonce(&mut self, resource_nonce: Vec<u8>) -> &mut Self {
        self.resource_nonce = Some(resource_nonce);
        self
    }
    #[inline(always)]
    pub fn proof(&mut self, proof: Vec<[u8; 32]>) -> &mut Self {
        self.proof = Some(proof);
        self
    }
    /// Add an aditional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = ProveClaim {
            distributor: self.distributor.expect("distributor is not set"),
            claim_proof: self.claim_proof.expect("claim_proof is not set"),
            temporal: self.temporal.expect("temporal is not set"),
            payer: self.payer.expect("payer is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_program::pubkey!("11111111111111111111111111111111")),
        };
        let args = ProveClaimInstructionArgs {
            claim_prefix: self.claim_prefix.clone().expect("claim_prefix is not set"),
            claim_bump: self.claim_bump.clone().expect("claim_bump is not set"),
            index: self.index.clone().expect("index is not set"),
            amount: self.amount.clone().expect("amount is not set"),
            claimant_secret: self
                .claimant_secret
                .clone()
                .expect("claimant_secret is not set"),
            resource: self.resource.clone().expect("resource is not set"),
            resource_nonce: self
                .resource_nonce
                .clone()
                .expect("resource_nonce is not set"),
            proof: self.proof.clone().expect("proof is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `prove_claim` CPI accounts.
pub struct ProveClaimCpiAccounts<'a, 'b> {
    /// The [MerkleDistributor].
    pub distributor: &'b solana_program::account_info::AccountInfo<'a>,
    /// Status of the claim.
    pub claim_proof: &'b solana_program::account_info::AccountInfo<'a>,
    /// Extra signer expected for claims
    pub temporal: &'b solana_program::account_info::AccountInfo<'a>,
    /// Payer of the claim.
    pub payer: &'b solana_program::account_info::AccountInfo<'a>,
    /// The [System] program.
    pub system_program: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `prove_claim` CPI instruction.
pub struct ProveClaimCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,
    /// The [MerkleDistributor].
    pub distributor: &'b solana_program::account_info::AccountInfo<'a>,
    /// Status of the claim.
    pub claim_proof: &'b solana_program::account_info::AccountInfo<'a>,
    /// Extra signer expected for claims
    pub temporal: &'b solana_program::account_info::AccountInfo<'a>,
    /// Payer of the claim.
    pub payer: &'b solana_program::account_info::AccountInfo<'a>,
    /// The [System] program.
    pub system_program: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ProveClaimInstructionArgs,
}

impl<'a, 'b> ProveClaimCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: ProveClaimCpiAccounts<'a, 'b>,
        args: ProveClaimInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            distributor: accounts.distributor,
            claim_proof: accounts.claim_proof,
            temporal: accounts.temporal,
            payer: accounts.payer,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.distributor.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.claim_proof.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.temporal.key,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.payer.key,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = ProveClaimInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::GUMDROP_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + 1 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.distributor.clone());
        account_infos.push(self.claim_proof.clone());
        account_infos.push(self.temporal.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ProveClaim` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` distributor
///   1. `[writable]` claim_proof
///   2. `[signer]` temporal
///   3. `[writable, signer]` payer
///   4. `[]` system_program
pub struct ProveClaimCpiBuilder<'a, 'b> {
    instruction: Box<ProveClaimCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ProveClaimCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ProveClaimCpiBuilderInstruction {
            __program: program,
            distributor: None,
            claim_proof: None,
            temporal: None,
            payer: None,
            system_program: None,
            claim_prefix: None,
            claim_bump: None,
            index: None,
            amount: None,
            claimant_secret: None,
            resource: None,
            resource_nonce: None,
            proof: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [MerkleDistributor].
    #[inline(always)]
    pub fn distributor(
        &mut self,
        distributor: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.distributor = Some(distributor);
        self
    }
    /// Status of the claim.
    #[inline(always)]
    pub fn claim_proof(
        &mut self,
        claim_proof: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.claim_proof = Some(claim_proof);
        self
    }
    /// Extra signer expected for claims
    #[inline(always)]
    pub fn temporal(
        &mut self,
        temporal: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.temporal = Some(temporal);
        self
    }
    /// Payer of the claim.
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_program::account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    /// The [System] program.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn claim_prefix(&mut self, claim_prefix: Vec<u8>) -> &mut Self {
        self.instruction.claim_prefix = Some(claim_prefix);
        self
    }
    #[inline(always)]
    pub fn claim_bump(&mut self, claim_bump: u8) -> &mut Self {
        self.instruction.claim_bump = Some(claim_bump);
        self
    }
    #[inline(always)]
    pub fn index(&mut self, index: u64) -> &mut Self {
        self.instruction.index = Some(index);
        self
    }
    #[inline(always)]
    pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.instruction.amount = Some(amount);
        self
    }
    #[inline(always)]
    pub fn claimant_secret(&mut self, claimant_secret: Pubkey) -> &mut Self {
        self.instruction.claimant_secret = Some(claimant_secret);
        self
    }
    #[inline(always)]
    pub fn resource(&mut self, resource: Pubkey) -> &mut Self {
        self.instruction.resource = Some(resource);
        self
    }
    #[inline(always)]
    pub fn resource_nonce(&mut self, resource_nonce: Vec<u8>) -> &mut Self {
        self.instruction.resource_nonce = Some(resource_nonce);
        self
    }
    #[inline(always)]
    pub fn proof(&mut self, proof: Vec<[u8; 32]>) -> &mut Self {
        self.instruction.proof = Some(proof);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = ProveClaimInstructionArgs {
            claim_prefix: self
                .instruction
                .claim_prefix
                .clone()
                .expect("claim_prefix is not set"),
            claim_bump: self
                .instruction
                .claim_bump
                .clone()
                .expect("claim_bump is not set"),
            index: self.instruction.index.clone().expect("index is not set"),
            amount: self.instruction.amount.clone().expect("amount is not set"),
            claimant_secret: self
                .instruction
                .claimant_secret
                .clone()
                .expect("claimant_secret is not set"),
            resource: self
                .instruction
                .resource
                .clone()
                .expect("resource is not set"),
            resource_nonce: self
                .instruction
                .resource_nonce
                .clone()
                .expect("resource_nonce is not set"),
            proof: self.instruction.proof.clone().expect("proof is not set"),
        };
        let instruction = ProveClaimCpi {
            __program: self.instruction.__program,

            distributor: self
                .instruction
                .distributor
                .expect("distributor is not set"),

            claim_proof: self
                .instruction
                .claim_proof
                .expect("claim_proof is not set"),

            temporal: self.instruction.temporal.expect("temporal is not set"),

            payer: self.instruction.payer.expect("payer is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

struct ProveClaimCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    distributor: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    claim_proof: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    temporal: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    claim_prefix: Option<Vec<u8>>,
    claim_bump: Option<u8>,
    index: Option<u64>,
    amount: Option<u64>,
    claimant_secret: Option<Pubkey>,
    resource: Option<Pubkey>,
    resource_nonce: Option<Vec<u8>>,
    proof: Option<Vec<[u8; 32]>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
