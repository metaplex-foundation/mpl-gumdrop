/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseDistributorInstructionAccounts = {
  /** Base key of the distributor. */
  base: Signer;
  /** [MerkleDistributor]. */
  distributor: PublicKey | Pda;
  distributorWallet: PublicKey | Pda;
  /** Who is receiving the remaining tokens and rent allocations. */
  receiver: PublicKey | Pda;
  /** The [System] program. */
  systemProgram?: PublicKey | Pda;
  /** SPL [Token] program. */
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type CloseDistributorInstructionData = {
  discriminator: Array<number>;
  bump: number;
  walletBump: number;
};

export type CloseDistributorInstructionDataArgs = {
  bump: number;
  walletBump: number;
};

export function getCloseDistributorInstructionDataSerializer(): Serializer<
  CloseDistributorInstructionDataArgs,
  CloseDistributorInstructionData
> {
  return mapSerializer<
    CloseDistributorInstructionDataArgs,
    any,
    CloseDistributorInstructionData
  >(
    struct<CloseDistributorInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['bump', u8()],
        ['walletBump', u8()],
      ],
      { description: 'CloseDistributorInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [202, 56, 180, 143, 46, 104, 106, 112],
    })
  ) as Serializer<
    CloseDistributorInstructionDataArgs,
    CloseDistributorInstructionData
  >;
}

// Args.
export type CloseDistributorInstructionArgs =
  CloseDistributorInstructionDataArgs;

// Instruction.
export function closeDistributor(
  context: Pick<Context, 'programs'>,
  input: CloseDistributorInstructionAccounts & CloseDistributorInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'gumdrop',
    'gdrpGjVffourzkdDRrQmySw4aTHr8a3xmQzzxSwFD1a'
  );

  // Accounts.
  const resolvedAccounts = {
    base: { index: 0, isWritable: false as boolean, value: input.base ?? null },
    distributor: {
      index: 1,
      isWritable: true as boolean,
      value: input.distributor ?? null,
    },
    distributorWallet: {
      index: 2,
      isWritable: true as boolean,
      value: input.distributorWallet ?? null,
    },
    receiver: {
      index: 3,
      isWritable: false as boolean,
      value: input.receiver ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    tokenProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CloseDistributorInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseDistributorInstructionDataSerializer().serialize(
    resolvedArgs as CloseDistributorInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
